.terraform/
.artifacts/
__pycache__/
# Local operator inputs/state — terraform.tfvars contains secrets, never commit.
terraform.tfvars
*.tfstate
*.tfstate.*
*.tfplan
# Apply-time build artifacts for the PostGIS bootstrap Lambda.
stacks/aws/postgis-bootstrap/build/
stacks/aws/postgis-bootstrap/bootstrap.zip
# Credential-safe candidate-preflight Lambda package.
stacks/aws-candidate-preflight/candidate-preflight.zip
# Governed database-migration runner build/package artifacts.
stacks/aws-db-migration-runner/build/
stacks/aws-db-migration-runner/db-migration-runner.zip
